[
  {
    "label": "p2: the chosen editor is ignored",
    "file": "lib/core/utils/file_actions.dart",
    "find": "    var chosenFailed = false;\n    if (bundleId.isNotEmpty) {\n      final chosen = await launch('open', ['-b', bundleId, ...absolutePaths]);\n      if (chosen.exitCode == 0) return;\n      chosenFailed = true;\n    }\n",
    "replace": "    const chosenFailed = false;\n",
    "tests": [
      "test/file_actions_test.dart"
    ]
  },
  {
    "label": "p2: a failed chosen editor is not retried with the default",
    "file": "lib/core/utils/file_actions.dart",
    "find": "      final chosen = await launch('open', ['-b', bundleId, ...absolutePaths]);\n      if (chosen.exitCode == 0) return;\n      chosenFailed = true;",
    "replace": "      await launch('open', ['-b', bundleId, ...absolutePaths]);\n      return;",
    "tests": [
      "test/file_actions_test.dart"
    ]
  },
  {
    "label": "p2: the fallback is silent about the editor it could not use",
    "file": "lib/core/utils/file_actions.dart",
    "find": "    if (!chosenFailed) return;\n    onNotice?.call(\n      'Could not open with $bundleId \u2014 used the system default instead.',\n    );",
    "replace": "    return;",
    "tests": [
      "test/file_actions_test.dart"
    ]
  },
  {
    "label": "p2: the terminal launch ignores its exit status",
    "file": "lib/core/utils/file_actions.dart",
    "find": "    if (result.exitCode == 0) return;\n    throw FileOpenException([path], '${result.stderr}'.trim());",
    "replace": "    return;",
    "tests": [
      "test/file_actions_test.dart"
    ]
  },
  {
    "label": "p1: a bundle without an identifier is accepted",
    "file": "lib/core/utils/app_bundle.dart",
    "find": "  if (id.isEmpty) return null;\n",
    "replace": "",
    "tests": [
      "test/app_bundle_test.dart"
    ]
  },
  {
    "label": "p3: the preferred editor never loads from prefs",
    "file": "lib/core/settings/app_settings.dart",
    "find": "      preferredEditorBundleId: prefs.getString(_editorBundleIdKey),\n      preferredEditorName: prefs.getString(_editorNameKey),\n",
    "replace": "",
    "tests": [
      "test/app_settings_test.dart",
      "test/preferred_apps_load_race_test.dart"
    ]
  },
  {
    "label": "p3: setPreferredApps changes state without persisting it",
    "file": "lib/core/settings/app_settings.dart",
    "find": "    await _persist((prefs) async {\n      await prefs.setString(_editorBundleIdKey, state.preferredEditorBundleId);\n      await prefs.setString(_editorNameKey, state.preferredEditorName);\n    });",
    "replace": "    return;",
    "tests": [
      "test/app_settings_test.dart"
    ]
  },
  {
    "label": "p3: readiness is never released, so a launch waits forever",
    "file": "lib/core/settings/app_settings.dart",
    "find": "    _applyFromPrefs(prefs, abort: () => !ref.mounted);\n    _storedApplied = true;\n    markSettingsLoaded();",
    "replace": "    _applyFromPrefs(prefs, abort: () => !ref.mounted);\n    _storedApplied = true;",
    "tests": [
      "test/preferred_apps_load_race_test.dart"
    ]
  },
  {
    "label": "p3: an in-flight write is never re-applied, so the load is lost",
    "file": "lib/core/settings/app_settings.dart",
    "find": "    if (!_storedApplied && ref.mounted) unawaited(_load());\n",
    "replace": "",
    "tests": [
      "test/preferred_apps_load_race_test.dart"
    ]
  }
]
