[
  {
    "label": "p1: the catch-all is removed, so four of five failures strand the slot",
    "file": "lib/core/git/remote_watch_service.dart",
    "find": "        } catch (_) {\n          // Idempotent (`armCounted`), so this is safe even on the paths that\n          // already released explicitly.\n          releaseSlot();",
    "replace": "        } catch (_) {",
    "tests": [
      "test/watch_slot_leak_test.dart"
    ]
  },
  {
    "label": "p1: the catch swallows, so a blip looks like a successful arm",
    "file": "lib/core/git/remote_watch_service.dart",
    "find": "          rethrow;\n        }",
    "replace": "          return const WatchUnavailable(WatchUnavailableReason.noTool);\n        }",
    "tests": [
      "test/watch_slot_leak_test.dart",
      "test/remote_watch_service_test.dart"
    ]
  },
  {
    "label": "p2: the cap is hardcoded again",
    "file": "lib/core/git/remote_watch_service.dart",
    "find": "    final derived = _streamBudget() - reservedStreams;\n    return derived < 1 ? 1 : derived;",
    "replace": "    return 2;",
    "tests": [
      "test/watch_ceiling_derived_test.dart",
      "test/remote_watch_service_test.dart"
    ]
  },
  {
    "label": "p2: the floor is removed, so a degraded session watches nothing",
    "file": "lib/core/git/remote_watch_service.dart",
    "find": "    return derived < 1 ? 1 : derived;",
    "replace": "    return derived;",
    "tests": [
      "test/watch_ceiling_derived_test.dart"
    ]
  },
  {
    "label": "p2: the reserved headroom is dropped, so watchers can starve the CI trace",
    "file": "lib/core/git/remote_watch_service.dart",
    "find": "  static const int reservedStreams = 2;",
    "replace": "  static const int reservedStreams = 0;",
    "tests": [
      "test/watch_ceiling_derived_test.dart"
    ]
  },
  {
    "label": "p2: the counter key ignores the session again",
    "file": "lib/core/git/remote_watch_service.dart",
    "find": "        final key = (_scope, host);",
    "replace": "        final key = (_noScope, host);",
    "tests": [
      "test/watch_ceiling_derived_test.dart",
      "test/watch_ceiling_per_host_test.dart"
    ]
  },
  {
    "label": "p2: the release announcement ignores the session",
    "file": "lib/core/git/remote_watch_service.dart",
    "find": "  static String _releaseKey(Object scope, String host) => '$scope\\u0000$host';",
    "replace": "  static String _releaseKey(Object scope, String host) => host;",
    "tests": [
      "test/watch_ceiling_derived_test.dart",
      "test/watch_ceiling_per_host_test.dart"
    ]
  },
  {
    "label": "p3: reconciliation clears everything regardless of the report",
    "file": "lib/core/git/remote_watch_service.dart",
    "find": "      final orphaned = held.difference(reportedLive);",
    "replace": "      final orphaned = held.toSet();",
    "tests": [
      "test/watch_reconcile_test.dart"
    ]
  },
  {
    "label": "p3: reconciliation is a no-op, so a stranded slot persists",
    "file": "lib/core/git/remote_watch_service.dart",
    "find": "    if (reported != null) _reconcileSlots(reported);",
    "replace": "",
    "tests": [
      "test/watch_reconcile_test.dart"
    ]
  },
  {
    "label": "p3: a failed sweep is treated as 'nothing is live'",
    "file": "lib/core/git/remote_watch_service.dart",
    "find": "    Set<String>? reported;",
    "replace": "    Set<String>? reported = <String>{};",
    "tests": [
      "test/watch_reconcile_test.dart"
    ]
  },
  {
    "label": "p3: one session reconciles every session's slots away",
    "file": "lib/core/git/remote_watch_service.dart",
    "find": "      if (key.$1 != _scope) continue;",
    "replace": "",
    "tests": [
      "test/watch_reconcile_test.dart"
    ]
  },
  {
    "label": "p3: the sweep stops reporting what is still leased",
    "file": "lib/core/git/bounded_watch.dart",
    "find": "      '[ -n \"\\$b\" ] && printf \\'LIVE %s\\\\n\\' \"\\$b\"; '",
    "replace": "      ': ; '",
    "tests": [
      "test/watcher_sweep_exec_test.dart",
      "test/watch_reconcile_test.dart"
    ]
  }
]